# Contributing

Keep provider-specific Kubernetes fields aligned with the exact official release. Do not add customer credentials, private keys, signed URLs, rendered customer manifests, or unverified architecture claims. Run `make validate`, shell syntax checks, and secret scanning before review.
